[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --timeout 600 --workers 1"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "sequence-video-tool"
